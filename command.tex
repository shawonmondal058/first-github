# one time each project to start git
git init
# every time to add 
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/shawonmondal058/first-github.git
git push -u origin main